---
import { ArrowRight, Layers } from "lucide-react";
import Terminal from "../ui/Terminal.astro";
---

<section
  class="min-h-dvh flex flex-col md:flex-row
  items-center justify-between px-4 md:px-16 lg:px-24 xl:px-32 py-24
  overflow-hidden
  bg-[#E9EEF5]
  bg-[radial-gradient(circle_at_72%_45%,rgba(56,189,248,0.28),transparent_60%)]"
>
    <div class="z-10 w-full md:w-10/12 mx-auto flex items-center justify-between gap-6">
            <div>
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-blue-700/30 bg-blue-500/10 text-blue-700 text-xs font-mono font-bold uppercase tracking-wider mb-2">
                    <span class="w-2 h-2 rounded-full bg-blue-700 animate-pulse"></span>
                    Disponible para nuevos retos
                </div>

                <h1 class="text-center md:text-left text-5xl md:text-6xl font-semibold max-w-xl leading-tight">
                    No escribimos código.
                    <span class="bg-linear-to-r">
                        Diseñamos el Futuro.
                    </span>
                </h1>

                <p class="text-center md:text-left text-slate-600 max-w-lg text-base">
                    Agencia de desarrollo de software de alto rendimiento. Transformamos ideas complejas en experiencias digitales escalables, rápidas y visualmente impactantes.
                </p>

                <div class="flex items-center gap-4 mt-6">
                    <button class="group px-6 h-11 rounded-md bg-blue-500 text-white font-bold transition-all hover:bg-blue-500 hover:shadow-[0_0_30px_rgba(59,130,246,0.5)] flex items-center justify-center gap-2">
                        Agendar Consultoría
                        <ArrowRight size={18} className="group-hover:translate-x-1 transition-transform" />
                    </button>

                    <button
                        class="flex items-center gap-2 rounded-md px-6 h-11 border border-slate-300 bg-white/70 backdrop-blur hover:bg-white transition active:scale-95"
                    >
                        <Layers size={18} />
                        <span>Ver proyectos</span>
                    </button>
                </div>
            </div>
            <Terminal />
        </div>
    </div>
</section>
