---
import { Picture } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import bannerMovil from "../assets/banner-movil.png";
import bannerDesktop from "../assets/banner-desktop.png";
import ContactForm from "../components/contact/ContactForm";
---

<Layout title="Contacto">
    <div class="relative w-full h-64 md:h-72 lg:h-80 overflow-hidden">
        <Picture
            src={bannerMovil}
            alt="Contacto TriCode Studio"
            formats={["avif", "webp"]}
            class="w-full h-full md:hidden object-cover object-center"
        />

        <Picture
            src={bannerDesktop}
            alt="Contacto TriCode Studio"
            formats={["avif", "webp"]}
            class="w-full h-full hidden md:block object-cover object-center"
        />

        <div class="absolute inset-0 bg-linear-to-t from-black/80 via-black/60 to-black/30"></div>

        <div class="absolute inset-0 flex flex-col items-center justify-center text-center px-6">
            <span class="text-sm tracking-widest uppercase font-semibold text-blue-300 mb-3">
                Nuestra esencia
            </span>

            <h1 class="text-white text-2xl md:text-4xl font-extrabold tracking-tight uppercase">
                Contáctanos
            </h1>

            <div class="mt-4 h-1 w-20 bg-blue-400/60"></div>
        </div>
    </div>

    <section class="max-w-7xl mx-auto py-10 px-5 lg:px-0 text-center space-y-5">
        <p class="max-w-5xl mx-auto text-2xl md:text-4xl font-bold uppercase">
            Una agencia de desarrollo de software enfocada a empresas ambiciosas
        </p>
        <p class="max-w-6xl mx-auto text-gray-900">
            Si buscas potenciar tu negocio con tecnología, nuestro equipo de
            desarrollo de software está listo para crear soluciones digitales a
            medida, escalables y eficientes que impulsen tus procesos y aceleren
            tu crecimiento.
        </p>
    </section>

    <section class="min-h-[70vh] flex items-center">
        <div class="z-10 w-full space-y-6">
            <h3 class="text-lg md:text-2xl font-bold text-center ">
                ¿Te gustaría trabajar con nosotros?
            </h3>
            <div
                class="grid grid-cols-1 lg:grid-cols-2 max-w-7xl mx-auto gap-10 px-6"
            >
                <div class="flex items-center">
                    <p class="text-base lg:text-xl">
                        Si estás listo para dar el siguiente paso, completa
                        nuestro formulario y cuéntanos sobre tu proyecto.
                        Analizaremos tus necesidades para diseñar una solución
                        tecnológica a tu medida y responderemos cualquier duda a
                        la brevedad.
                    </p>
                </div>

                <div>
                    <ContactForm client:only="react" />
                </div>
            </div>
        </div>
    </section>
</Layout>
